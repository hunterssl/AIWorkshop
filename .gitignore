# ========================================
# AIWorkshop .gitignore
# ========================================

# ---------- 构建产物 ----------
studio/dist/
canvas/dist/
*.pyc
*.pyo
*.pyd

# ---------- 依赖 ----------
node_modules/

# ---------- 系统文件 ----------
.DS_Store
Thumbs.db
.code-workspace

# ---------- 用户数据（运行时产生） ----------
studio_users/

# ---------- 机器相关配置（迁移时需手动设置） ----------
# 以下文件包含本机绝对路径，不应提交到仓库。
# 迁移到新机器时，参照 config/paths.example.js 创建。
config/paths.js
canvas/virtual_image_library.json